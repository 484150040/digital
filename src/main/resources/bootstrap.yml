spring:
  profiles:
    active: digital
  application:
    name: digital-nacos
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.134:8848
      config:
        server-addr: 192.168.0.134:8848
        file-extension: yml
        group: DEFAULT_GROUP
  main:
    allow-bean-definition-overriding: true
management:
  endpoints:
    web:
      exposure:
        include: refresh,health